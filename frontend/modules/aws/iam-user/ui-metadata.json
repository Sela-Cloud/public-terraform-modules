{
  "$schema": "../../ui-metadata.schema.json",
  "module": "iam-user",
  "version": "1.0.0",
  "display": {
    "label": "AWS IAM User",
    "icon": "iam",
    "description": "Provision AWS IAM Users with custom paths, permissions boundaries, and tags",
    "category": "global"
  },
  "globals": [
    {
      "id": "region",
      "label": "AWS Region",
      "description": "The AWS region where resources will be provisioned.",
      "type": "text",
      "required": true,
      "default": "us-east-1",
      "placeholder": "us-east-1"
    }
  ],
  "resource": {
    "variable": "iam_user",
    "key_field": "name",
    "key_label": "User Name",
    "key_description": "Unique username for the IAM user.",
    "sections": [
      {
        "id": "basic",
        "label": "Basic Configuration",
        "description": "Core IAM user settings including username and path hierarchy.",
        "fields": [
          {
            "id": "name",
            "label": "User Name",
            "description": "The user's name consisting of alphanumeric characters and symbols +=,.@_-",
            "type": "text",
            "required": true,
            "default": "iam-user-default",
            "placeholder": "john.doe",
            "validation": {
              "min": 1,
              "max": 64,
              "pattern": "^[a-zA-Z0-9+=,.@_-]+$",
              "pattern_error": "User name must be 1 to 64 characters and contain only alphanumeric characters and symbols +=,.@_-"
            }
          },
          {
            "id": "path",
            "label": "Path",
            "description": "Path in which to create the user. Must begin and end with a forward slash (/).",
            "type": "text",
            "required": false,
            "default": "/",
            "placeholder": "/",
            "validation": {
              "pattern": "^(/|(/[a-zA-Z0-9_+=,.@-]+)+/)$",
              "pattern_error": "Path must begin and end with a forward slash (/)."
            }
          }
        ]
      },
      {
        "id": "permissions",
        "label": "Permissions & Boundaries",
        "description": "Permission boundaries to restrict the maximum permissions this user can obtain.",
        "fields": [
          {
            "id": "permissions_boundary",
            "label": "Permissions Boundary ARN",
            "description": "The ARN of the managed policy that sets the permissions boundary for the user.",
            "type": "text",
            "required": false,
            "placeholder": "arn:aws:iam::123456789012:policy/DeveloperBoundary"
          }
        ]
      },
      {
        "id": "tags",
        "label": "Tags",
        "description": "Resource tags for organizational, tracking, and cost allocation purposes.",
        "fields": [
          {
            "id": "tags",
            "label": "User Tags",
            "description": "Key-value tags assigned to the IAM user.",
            "type": "map",
            "required": false,
            "default": {}
          }
        ]
      }
    ]
  }
}
